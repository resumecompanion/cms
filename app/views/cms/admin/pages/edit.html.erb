<div class="row">
  <div class="span12">
    <section>
      <div class="page-header">
        <h1>Edit Page</h1>
      </div>
    </section>

    <%= simple_form_for @page, :url => cms.admin_page_path(@page), :method => "put" do |f| %>
      <%= render "form", :f => f %>

      <div class="form-actions">
        <%= f.submit "Upage Page", :class => "btn btn-primary" %>
        <%= link_to "Cancel", cms.admin_pages_path, :class => "btn" %>
      </div>
    <% end %>
  </div>
</div>