<div class="row">
  <div class="span12">
    <section>
      <div class="page-header">
        <h1>Edit Sidebar</h1>
      </div>
    </section>

    <%= simple_form_for @sidebar, :url => cms.admin_sidebar_path(@sidebar), :method => "put" do |f| %>
      <%= render "form", :f => f %>

      <div class="form-actions">
        <%= f.submit "Upage Sidebar", :class => "btn btn-primary" %>
        <%= link_to "Cancel", cms.admin_sidebars_path, :class => "btn" %>
      </div>
    <% end %>
  </div>
</div>